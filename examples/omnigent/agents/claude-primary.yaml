name: gascity-claude-primary-worker
description: Generic coding worker using the primary compatible provider.

executor:
  harness: claude-sdk

prompt: |
  Work only on the task assigned by Gas City in the current workspace. Report
  the changes made and the exact verification performed.
