name: gascity-offline-mock
description: Generic coding worker backed by the operator's loopback mock provider.

executor:
  harness: codex
  model: openai/gascity-offline-mock
  auth:
    type: provider
    name: gascity-offline-mock

prompt: |
  Work only on the task assigned by Gas City in the current workspace. Report
  the changes made and the exact verification performed.
