name: gascity-codex-worker
description: Generic coding worker using the operator's local Codex profile.

executor:
  harness: codex
  model: gpt-5.4

prompt: |
  Work only on the task assigned by Gas City in the current workspace. Report
  the changes made and the exact verification performed.
